# Copy to .env.local and adjust if the API runs elsewhere. .env.local is git-ignored.
# Base URL of the API without the /api suffix (it is appended in src/api/config.js).
# Local default matches apps/api PORT; on Vercel set the same variable in project settings.
VITE_API_URL="http://localhost:3000"
