import styles from '../CourseWizard.module.css';

const StepContentPlaceholder = () => (
  <div className={styles.placeholder}>Наповнення програми — наступний етап</div>
);

export default StepContentPlaceholder;
